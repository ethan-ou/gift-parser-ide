import mocks from "../../__tests__/mocks";
import { findToken } from "../index";
import parse from "../../parser";
import parser from "../../parser";

// describe("findToken()", () => {
//   it("returns nothing if no scopes on a single line", () => {
//     let result = parser(mocks[2].text);
//     expect(findToken()).toEqual([]);
//   });

//   it("returns if a scope is on a single line", () => {
//     expect(findToken(mocks[3].text)).toEqual(["SCOPE"]);
//   });
// });
